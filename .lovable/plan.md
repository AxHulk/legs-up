
## Что меняем

### 1. База данных (одна миграция)
- `DELETE` инструктора «Наталья Клочкова».
- `INSERT` три новых карточки с автосгенерированным описанием:
  - **Лилия Романова** — пилатес на большом и малом оборудовании
  - **Елизавета Кечина** — реабилитация, ЛФК, работа с беременными
  - **Анастасия Петрова** — хатха-йога, остеопрактик
- Пересчитать `sort_order` так, чтобы порядок был: Валентина Рудич → Наталья Чеботарёва → Мария Козлова → Мария Эрькина → Лилия Романова → Елизавета Кечина → Анастасия Петрова.

### 2. Фотографии
- Конвертирую HEIC-файлы новых тренеров в JPG в песочнице.
- Загружаю 6 изображений через Lovable Assets:
  - главные фото для Козловой, Эрькиной, Чеботарёвой (заменяю только `photos[0]`, вторая фотография для модалки остаётся);
  - фото для Лилии, Елизаветы, Анастасии.
- Прописываю URL в поле `photos` соответствующих записей.

### 3. Компонент `src/components/site/Team.tsx`
- Меняю сетку `grid-cols-5` на раскладку **4 + 3**:
  - `lg`: два ряда — `grid-cols-4` сверху и `grid-cols-3` (центрированный, уже) снизу;
  - `md`: 3 колонки; `sm`: 2 колонки.
- Пропорционально увеличиваю карточки (`max-w` портрета ~320 px вместо 260 px, крупнее шрифт имени).
- Обновляю карту `PHOTO_FRAMING`: убираю Клочкову, добавляю дефолты для трёх новых имён (подкорректирую после первого скриншота).

### 4. Проверка
- Playwright-скриншоты секции «Команда» на 1280 px и 390 px — проверить, что 4+3 выравнивается по центру, лица не обрезаются, скелетон/загрузка не ломают раскладку.

## Технические детали
- Миграция в `supabase/migrations/*_team_refresh.sql` — только `INSERT/UPDATE/DELETE` в `public.instructors`, RLS уже настроена.
- Ассеты кладу в `src/assets/team/*.jpg.asset.json`, но в БД сохраняю CDN-URL напрямую (как это уже сделано у существующих карточек).
- Тексты био — 3–4 предложения в текущем «мягком» тоне сайта.
